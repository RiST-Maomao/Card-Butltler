services:
  - name: battle-line-online
    type: web
    runtime: node
    plan: free
    rootDir: battle-line-online
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
